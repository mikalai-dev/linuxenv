- hosts: localhost
  roles:
    - role: "../roles/linuxenv/tasks/main.yml"
  tasks:
    - name: Install libvirt
      apt:
        pkg:
           - libvirtd
           - qemu-kvm
           - virt-manager
           - qemu-img
           - qemu-utils
      tags:
        - deb

    - name: Install libvirt
      community.general.pacman:
        name:
          - libvirt
          - qemu-full
          - virt-manager
      tags:
        - arch
    
    - name: Start libvirt
      service: libvirtd
      status: started
